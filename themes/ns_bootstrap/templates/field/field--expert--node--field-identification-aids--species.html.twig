{#
/**
 * @file
 * Template to provide expert HTML for the field.
 *
 * Available variables:
 * - element: The field element.
 * - label: The label of the field.
 * - settings: The settings for the field.
 * - items: List of all the field items. Each item contains:
 *   - attributes: List of HTML attributes for each item.
 *   - content: The field item's content.
 */
#}

{% for item in items %}
  {% set value = item.content["#text"]|trim %}
  <div class="field row">
    <div class="col-md-3 field-label-inline">{{ label }}</div>
    <div class="col-md-9">
      <button data-toggle="collapse" data-target="#ident-aid" class="btn btn-info">Show identification aids</button>
      <div id="ident-aid" class="collapse">
      {{ item.content|raw }}
      </div>
    </div>
  </div>
{%  endfor %}